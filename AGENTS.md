# 작업 입구

현재 작업은 [README](README.md)의 현재 단계·작업 경로에서 시작한다. 아래 정본 중 이번 결정에 필요한 절을 읽는다. 과거 대화나 인수인계 사본은 필수 입력이 아니다.

| 필요한 판단 | 정본 |
|---|---|
| 제품 목적·확장 순서·채택 조건·변경 영향 | [제품](docs/product.md) |
| 실험 설계·시간 원점·재사용 가설과 반례 | [연구](docs/research.md) |
| 보스별 내부 연결·관측·후보·현행 구간 | [보스 조사](docs/boss-control-patterns.md), [베스퍼](docs/experiments/vesper-control-skills.md), [기르타블리르](docs/experiments/girtablullu-control-skill.md), [쿠사리쿠](docs/experiments/kusarikku-control-skill.md), [환영의 화살 유닛](docs/experiments/arrow-unit-control-skill.md) |
| 도구 실행·기록과 정정 | [실험기 사용법](tools/experiment/사용법.md), [레인 검증기](tools/lane-verifier/사용법.md) |
| 현재 담당·미완료 판단·다음 행동 | [README](README.md#현재-단계--2026-09-21) |

## 작업과 근거

- controller는 사용자 목적에서 필요한 판단을 도출하고 범위·통합·최종 수용을 책임진다. 하위 구현의 완료나 최신 요청은 기존 목적의 철회가 아니다. 승인된 설정·구현 작업은 필요한 실제 사용 위치 반영과 검증까지 완료하며 보고만으로 멈추지 않는다. 실제로 막힌 판단·담당·대기 이유만 README에 유지하고, 에이전트가 수행할 작업과 사용자 실제 플레이로 확인할 사항을 구별한다.
- 이미 확정한 내용은 정본에서 찾는다. 질문은 답이 바꿀 결정이 있을 때 하며, 자료 대기는 막히는 작업만 특정하고 독립 작업을 이어간다. 종료한 보스를 새 반례 없이 다시 열거나 분석 누락을 사용자에게 발견하게 하지 않는다.
- 관측·조건부 예측·서비스 채택과 내부 좌표·실험 ms·원본 프레임·클립 초를 구별한다. 자료 확보·후보 판단·실행 준비·게임 성공·서비스 수용은 서로의 완료 근거가 아니다. 중요한 추론은 근거·반례가 선택과 다음 행동을 어떻게 바꾸는지 해당 보고서에 남긴다.
- 합의 변경은 담당 정본 한 곳에서 고치고 관련 구현·검사를 갱신한다. 조건부 합의를 단순 후순위로 축약하지 않는다. 이력은 당시 조건으로 보존하고 현행 규칙처럼 반복하지 않는다. 자료는 대표 성공·반례·정렬·서비스 원본 등 남은 역할에 따라 정리하며 삭제 경로를 재사용할 수 있다고 가정하지 않는다.
- 변경에 맞는 검사와 실제 산출물로 수용한다. 재생·해시·좌표·입력 제출 검사를 게임 성공이나 학습 효과로 보고하지 않는다. 통과 뒤 새 변경·실패가 없으면 검사를 반복 확대하지 않는다.

## 권한과 분업

- 게임 원본은 읽기 전용이다. 실제 게임 입력은 사용자가 명시적으로 켜고 수행한다. 에이전트는 가짜 입력·저장 영상·무입력 모드로 검증한다. 원시 감지·입력·녹화 증거를 변경하지 않으며 승인된 결과/시작 캐릭터 정정은 사용법의 출처·해시·이력 절차를 따른다. 원격 공개·배포는 별도 요청에 따른다.
- 비용이 큰 조사·구현 또는 제품 의미를 바꾸는 범위 선택은 별도 읽기 전용 검토자가 원요청·제품 목적·근거에서 필요한 판단을 먼저 도출한 뒤 실행안과 대조한다. controller는 의견을 근거로 수용·기각한다. 합의된 작은 실행마다 추가 검토나 승인을 만들지 않는다.
- 독립 질문이나 문맥을 압도하는 조사량에는 sub-agent를 사용하고 작은 연결 수정은 직접 한다. 모델·추론 수준은 난이도·오판 영향에 맞추며 단순 조사까지 최고 수준으로 고정하지 않는다. 위임에는 목적·판단할 질문·정본·소유 파일·권한·완료 근거를 준다. 검토는 읽기 전용으로 맡기며 구현 권한은 범위 확정 뒤 명시한다. 다른 작업자의 변경을 보존하며 같은 파일 수정·Git 변경·공유 상태 검사는 순서대로 한다. 별도 task는 사용자가 요청한 지속 역할에 사용한다.
- 담당자는 결론·출처·실제 변경/검사·반례·미확인을 반환한다. controller는 결정을 좌우하는 원근거와 산출물을 확인하며 같은 조사 전체를 반복하지 않는다. 자기 검토·독립 검토·교정 후 재시도를 구별한다.
- 실행 명령은 package.json과 tools.ps1을 따른다. 개인 경로나 전역 PATH 설정을 구현에 넣지 않는다. 실제 Project 경로·Git 상태·포트를 확인하고 다른 checkout의 서버를 재사용·종료하지 않는다. Codex 오른쪽 패널은 자동으로 열지 않는다.

## Controller 교체

이 절은 책임자를 실제로 교체할 때만 적용한다. 수용된 controller의 일상 조사·구현이나 sub-agent 결과 통합마다 인수 평가·추가 승인을 반복하지 않는다.

1. 기존 담당자는 README에 실제 경로·Git 기준점·미커밋 변경·원래 목적의 남은 판단과 담당을 유지한다. 새 task에는 이 입구와 실제 다음 작업을 제공한다. 별도 정답 사본을 만들지 않는다.
2. 후보는 관련 정본을 읽고 다음 판단을 수행한다. 검토자는 원래 목적에서 도출한 의무와 실제 출처·계산·선택·사용자 요청을 대조한다. 문서 요약·기술 검사·한 번 지적한 뒤의 정답을 인수 완료로 세지 않는다.
3. 중요한 교정 후에는 새로운 문맥에서 후속 관측의 재판단과 제한된 실제 산출물 갱신까지 확인한다. 가정한 결과는 실제 게임 기록에 넣지 않는다. 공용 파일은 검토 전 읽기 전용이며, 검증용 변경은 격리된 산출물 또는 명시적으로 한정한 파일에만 허용한다.
4. 수용 후 task·경로·맡은 범위·남은 판단을 README에서 갱신하고 변경 책임을 명시적으로 넘긴다. 한 파일의 책임자는 한 명이다. checker와 평가 task는 자동으로 controller가 되지 않는다. 이전 담당자가 없으면 독립 검토자를 사용하며 사용자에게 기존 합의 복원을 요구하지 않는다.

평가 방법과 한계는 [연구의 재개 평가](docs/research.md#재개-평가-방법)를 따른다. 검증은 실제 범위의 근거이며 모든 후속 작업의 보증이 아니다.
